{
  "name": "fyp-selenium-server",
  "version": "1.0.0",
  "description": "Server for my final year project",
  "main": "nightwatch.conf.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node_modules/.bin/nightwatch --config nightwatch.conf.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^2.0.0"
  }
}
